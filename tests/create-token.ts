import { web3 } from "@coral-xyz/anchor"
import { createMint } from "@solana/spl-token"

const createFeeToken = (connection:web3.Connection, mintAuthority: web3.PublicKey, freezeAuthority: web3.PublicKey)=>{
}